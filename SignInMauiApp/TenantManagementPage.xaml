<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SignInMauiApp.TenantManagementPage">
    <VerticalStackLayout Padding="20,30" Spacing="10">
        <Label Text="租户管理" FontSize="22" HorizontalOptions="Center" />
        <Entry x:Name="NewTenantEntry" Placeholder="新租户名称" />
        <Button Text="添加租户" Clicked="OnAddTenantClicked" />
        <CollectionView x:Name="TenantCollectionView" SelectionMode="Single" SelectionChanged="OnTenantSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="0,5" Padding="10" BorderColor="#ccc">
                        <HorizontalStackLayout>
                            <Label Text="{Binding Name}" VerticalOptions="Center" />
                            <Button Text="编辑" Clicked="OnEditTenantClicked" CommandParameter="{Binding .}" />
                            <Button Text="删除" Clicked="OnDeleteTenantClicked" CommandParameter="{Binding .}" />
                        </HorizontalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
